apiVersion: projctl.konflux.dev/v1beta1
kind: ProjectDevelopmentStream
metadata:
  name: operator
spec:
  project: operator
  template:
    name: operator-template
    values:
      - name: version
        value: "v1.2"
      - name: branch
        value: "release-1.2"
