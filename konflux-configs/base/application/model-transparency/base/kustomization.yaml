apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - application.yaml
  - component.yaml
  - imagerepository.yaml

components:
  - ../../base/application

patches:
  - path: patch/releaseplan.yaml
    target:
      kind: ReleasePlan
      name: promote-to-candidate-model-transparency
