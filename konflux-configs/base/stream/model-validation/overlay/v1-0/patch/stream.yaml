apiVersion: projctl.konflux.dev/v1beta1
kind: ProjectDevelopmentStream
metadata:
  name: "dummy"
spec:
  template:
    values:
      - name: version
        value: "v1.0"
      - name: branch
        value: "release-1.0"
