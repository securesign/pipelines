apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

nameSuffix: -sachin/test-pkcs11-e2e

components:
  - ../../base/

configurations:
  - kustomizeconfig.yaml

patches:
  - path: patch/exclude-segment-backup-job.yaml