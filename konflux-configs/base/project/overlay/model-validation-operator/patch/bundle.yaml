- op: add
  path: /spec/resources/-
  value:
    apiVersion: appstudio.redhat.com/v1alpha1
    kind: Component
    metadata:
      name: "model-validation-operator-bundle{{.nameSuffix}}"
    spec:
      application: "{{.application}}{{.nameSuffix}}"
      componentName: "model-validation-operator-bundle{{.nameSuffix}}"
      source:
        git:
          context: ./
          dockerfileUrl: bundle.Dockerfile
          revision: "{{.branch}}"
          url: https://github.com/securesign/model-validation-operator
