- op: add
  path: /spec/resources/-
  value:
    apiVersion: appstudio.redhat.com/v1alpha1
    kind: Component
    metadata:
      annotations:
        mintmaker.appstudio.redhat.com/disabled: "{{.mintmakerDisabled}}"
      name: "policy-controller-operator{{.nameSuffix}}"
    spec:
      application: "{{.application}}{{.nameSuffix}}"
      componentName: "policy-controller-operator{{.nameSuffix}}"
      source:
        git:
          context: ./
          dockerfileUrl: Dockerfile
          revision: "{{.branch}}"
          url: https://github.com/securesign/policy-controller-operator
